# Duplicado do conteudo descomprimido (84 MB) - nao versionar
download (12).zip
*.zip

# OS / editor junk
Thumbs.db
.DS_Store
desktop.ini

# Logs / temp
*.log
*.tmp

# Bases de dados SAO versionadas de proposito (o admin descarrega do repo para o server).
# Nao ignorar *.db / *.mv.db / *.sqlite.

# Area de trabalho descompilada (nunca deve existir aqui)
auditoria-pdc/
src-*/

# Backups locais da migracao
_backups/

# Documentacao/notas locais (nao versionar)
MEMORIA/