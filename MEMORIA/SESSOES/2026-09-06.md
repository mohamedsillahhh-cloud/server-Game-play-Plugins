# SESSÕES — 2026-09-06

> Sessão inicial de auditoria e criação da memória permanente.

## Objetivo
- Auditar o estado real do pacote de plugins do servidor Minecraft e criar a estrutura de memória **MEMORIA/** em `SERVER PLUGINS`.

## Data de referência
- Sessão em 2026-09-06 (confirmado por mtimes dos ficheiros: `SurvivalProgression-1.0.1-FIXED.jar` 01:07, `LEIA-ME-SURVIVALPROGRESSION.txt` 00:41, `SpawnerSpawnPoint`/`loerx-communityshop` 09-04).

## O que foi feito
1. **Inventário completo** de `download (12)\plugins` (30 jars, 30 pastas de config, 879 ficheiros).
2. **Leitura de documentação:** `README.TXT` (CombatLogX), `WIPE-LEIA-ME.txt`, `LEIA-ME-SURVIVALPROGRESSION.txt`.
3. **Leitura/extração de configs:** SurvivalProgression (config+data), SpawnerSpawnPoint, Baltop, KillMoney, RichReveal, SpruceBounty, Bounty (legacy), HomesPanel, loerx-communityshop (config/prices/stock/slots/templates/custom_shops/lang), CombatLogX (config/commands/punish), TAB (config/groups/users/messages), LuckPerms (config), WorldGuard (config+regiões), WorldEdit, EssentialsX (config/kits/worth/motd/spawn/tpr), LoginSecurity (config/database), Vault, PlaceholderAPI, ProtocolLib, PlayerHeadDrops, Shopkeepers, SkinsRestorer, WildStacker, InfinityStones, BlueSlimeCore, bStats, spark, voicechat, ChestCommands/DeluxeMenus.
4. **Inspeção de `plugin.yml` dentro dos jars custom** (nome/main/api-version/comandos/permissões).
5. **Verificação de bases de dados** (H2/SQLite — todas recriadas vazias pós-wipe).
6. **Criação de MEMORIA/** (README, ESTADO_ATUAL, ARQUITETURA, CONFIGURACAO/, DATABASE/, DECISOES/, DISCORD/, FLUXOS/, FUNCIONALIDADES/, HISTORICO/, PROBLEMAS/, SESSOES/).

## Descobertas principais
- O pacote é só **config/jars** — sem worlds, sem código-fonte, sem git.
- Wipe já aplicado → BDs vazias; **LuckPerms (grupos) e LoginSecurity (contas) e Shopkeepers (lojas) e WG (regiões) precisam de recriação** antes do servidor ficar "público".
- SurvivalProgression é o núcleo da progressão (homes, licenças, spawners via `/shop`, anti-killfarm).
- Comunidade: loja comunitária com stock/preços dinâmicos; bounties via SpruceBounty.

## Problemas encontrados (ver PROBLEMAS/)
- P-001 CombatLogX.zip a mais · P-002 PlaceholderAPI duplicado · P-003 HomesPanel `.disabled` (OK) · P-004 Bounty órfã · P-005 DeluxeMenus sem menus · P-006 caracteres corrompidos · P-007 preço crate 100k vs 500k · P-008 TAB proxy ligado sem proxy · P-009 ranks TAB vs SP desalinhados · P-010 softdeps ausentes · P-011 mundos de config vs reais.

## Decisões
- D-001…D-005 registadas em `DECISOES/README.md`.

## Recomendação / próxima vez
1. Recriar grupos/permissões no LuckPerms (alianha com ranks do SurvivalProgression + ordem do TAB).
2. Resolver P-001, P-002, P-005 (limpeza e DeluxeMenus).
3. Validar em runtime: spawners/licenças no `/shop`, comportamento da StoneCrate (P-007) e stock do CommunityShop.
4. Decidir nomes finais dos mundos (P-011) e desligar proxy do TAB (P-008).

---
Registo da sessão fechado às: — (aberto)